/** Provided by the `local-triage-summary` plugin in vite.config.ts. */
declare module 'virtual:triage-summary' {
  import type { TriagedEmail } from '@/lib/types'
  const summary: TriagedEmail[]
  export default summary
}
